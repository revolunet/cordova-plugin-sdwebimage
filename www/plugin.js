var exec = require("cordova/exec");

var SDWebImage = function(){};

SDWebImage.prototype.getImage = function (url) {
    
};


SDWebImage.prototype.init = function(options) {
    options = options || {};
};


module.exports = new SDWebImage();
